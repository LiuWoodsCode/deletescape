class App:
    def __init__(self, window, container):
        self.window = window
        self.container = container

        print("This app does nothing, this is just the app that deletescape pretends it's notifications are from")